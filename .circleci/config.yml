version: 2
jobs:
  build:
    docker:
      - image: ubuntu:16.04
    steps:
      - checkout
      - run:
          name: bhaskar 
          command: echo Hello, world.
      - run:
          name: Print the Current Time
          command: date
