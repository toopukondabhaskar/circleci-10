version: 2
jobs:
  deploy:
    docker:
    - image: circleci/ruby:2.4
    steps:
    - checkout
    - run: bundle install
  #build2:
    #docker:
    #- image: circleci/ruby:2.4
    #steps:
    #- checkout
    #- run: bundle install
#workflows:
  #version: 2
  #my_workflow:
    #jobs:
    #- build
    #- build2

workflows:
  version: 2
  build-deploy:
    jobs:
      - deploy-staging:
          requires:
            - build
            - test
          filters:
            branches:
              only: staging
          context: staging

      - deploy-prod:
          requires:
            - build
            - test
          filters:
            branches:
              only: master
          context: prodution
