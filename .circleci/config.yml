version: 2
jobs:
  build:
    docker:
    - image: dockerimages/latest
    steps:
    - checkout
    #- run: bundle install
    - run: docker run toopukonda/dockerimages latest
  build2:
    docker:
    - image: circleci/ruby:2.4
    steps:
    - checkout
    - run: bundle install
workflows:
  version: 2
  my_workflow:
    jobs:
    - build
    - build2
